name: Deploy admin → admin1 (GitHub Pages)

on:
  push:
    branches: [main]
    paths:
      - 'admin/**'
      - 'data/**'
      - '.github/workflows/deploy-admin.yml'
  workflow_dispatch:

permissions:
  contents: write   # нужно для записи в ветку gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. pnpm ────────────────────────────────────────────────────────────
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      # ── 3. Node ────────────────────────────────────────────────────────────
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: admin/pnpm-lock.yaml

      # ── 4. Install ─────────────────────────────────────────────────────────
      - name: Install dependencies
        working-directory: admin
        run: pnpm install --frozen-lockfile

      # ── 5. Build ───────────────────────────────────────────────────────────
      # NEXT_BASE_PATH   — используется в next.config.ts (basePath / assetPrefix)
      # NEXT_PUBLIC_BASE_PATH — передаётся в браузер; apiFetch добавляет его к /api/*
      - name: Build static export
        working-directory: admin
        run: pnpm run build
        env:
          NEXT_STATIC_EXPORT: 'true'
          NEXT_BASE_PATH: '/${{ github.event.repository.name }}/admin1'
          NEXT_PUBLIC_BASE_PATH: '/${{ github.event.repository.name }}/admin1'

      # ── 6. Deploy ──────────────────────────────────────────────────────────
      # Публикует папку admin/out → ветка gh-pages, подпапка admin1/
      # keep_files: true — не удаляет соседние папки (web1/)
      - name: Deploy to GitHub Pages (admin1)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: admin/out
          destination_dir: admin1
          keep_files: true
